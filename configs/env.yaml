landscape_id: 4

rewards:
  victory: 100
  death: -100
  step: -1

# Optional stochasticity in environment rollouts (does NOT change DP tables).
# Standard deviation of zero-mean Gaussian noise added to each reward at step().
reward_noise_std: 0.5
